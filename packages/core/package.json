{
  "name": "@nulab/datadog-ai-guard-middleware-core",
  "version": "0.1.0",
  "description": "Internal shared core for @nulab/datadog-ai-guard-middleware-ai-v2 and @nulab/datadog-ai-guard-middleware-ai-v3. Not intended for direct use.",
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22"
  },
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      }
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm run clean && tsc",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "peerDependencies": {
    "dd-trace": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  }
}
